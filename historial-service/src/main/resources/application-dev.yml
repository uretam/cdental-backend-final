server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:33067/db_historial?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true